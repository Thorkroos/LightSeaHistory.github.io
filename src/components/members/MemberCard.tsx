import {Link} from 'react-router-dom';import type {Member} from '../../types/database';export function MemberCard({member}:{member:Member}){return <Link className="member-card" to={`/members/${member.id}`}>{member.avatar_url?<img src={member.avatar_url} alt={`${member.display_name}的头像`}/>:<div className="avatar">{member.display_name.slice(0,1)}</div>}<div><h3>{member.display_name}</h3>{member.current_status&&<span className="pill">{member.current_status}</span>}<p>{member.description||'暂无简介'}</p></div></Link>}
