import {Link} from 'react-router-dom';import {formatEventDate} from '../../lib/dates';import type {HistoryEvent} from '../../types/database';export function EventCard({event}: {event:HistoryEvent}){return <article className={`event-card ${event.is_featured?'featured':''}`}><div className="eyebrow">{event.category?.icon} {event.category?.name} · {formatEventDate(event)}</div><h3><Link to={`/events/${event.id}`}>{event.title}</Link></h3><p>{event.summary||'暂无摘要'}</p><div className="meta">{event.location&&<span>📍 {event.location}</span>}<span>重要度 {'●'.repeat(event.importance)}{'○'.repeat(5-event.importance)}</span></div></article>}
